<%= form_with(model: animal, local: true) do |form| %>
  <% if animal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(animal.errors.count, "error") %> prohibited this animal from being saved:</h2>

      <ul>
      <% animal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :animal_number %>
    <%= form.number_field :animal_number, id: :animal_animal_number %>
  </div>

  <div class="field">
    <%= form.label :species %>
    <%= form.text_field :species, id: :animal_species %>
  </div>

  <div class="field">
    <%= form.label :birthday %>
    <%= form.date_select :birthday, id: :animal_birthday %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
