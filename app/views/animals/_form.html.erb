<%= form_with(model: animal, local: true) do |form| %>
  <% if animal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(animal.errors.count, "error") %> prohibited this animal from being saved:</h2>

      <ul>
      <% animal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :animal_number, 'Número de animal' %>
    <%= form.number_field :animal_number, id: :animal_animal_number %>
  </div>

  <div class="form-group">
    <%= form.label :species, 'Especie' %>
    <%= form.select :species, ['bovino', 'ovino'] %>
  </div>
  <div class="form-group">
    <%= form.label :ranch, 'Hacienda' %>
    <%= form.select :ranch, ['sauces', 'laureles'] %>
  </div>
  <div class="form-group">
    <%= form.label :lot_id, 'Lote' %>
    <%= form.collection_select(:lot_id, Lot.all.order("ranch desc, species asc, number asc"), :id, :number_with_ranch)  %>
  </div>
  <div class="form-group">
    <%= form.label :status, 'Estado' %>
    <%= form.select :status, ['engorde', 'vendido', 'muerto', 'perdido'] %>
  </div>
<!--  <div class="form-group">
    <%= form.label :birthday, 'Nacimiento' %>
    <%= form.date_select :birthday, id: :animal_birthday %>
  </div>-->

  <div class="form-group">
    <%= form.submit %>
  </div>
<% end %>
