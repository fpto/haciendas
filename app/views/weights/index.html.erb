<p id="notice"><%= notice %></p>

<div class = "container">
  <h1>Pesos</h1>

  <table class="table">
    <thead>
      <tr>
        <th>Animal</th>
        <th>Fecha</th>
        <th>Peso</th>
        <th>Nota</th>
        <th colspan="3">Acciones</th>
      </tr>
    </thead>

    <tbody>
      <% @weights.each do |weight| %>
        <tr>
          <td><%= weight.animal_id %></td>
          <td><%= weight.date %></td>
          <td><%= weight.weight %></td>
          <td><%= weight.note %></td>
          <td><%= link_to 'Mostrar', weight %></td>
          <td><%= link_to 'Editar', edit_weight_path(weight) %></td>
          <td><%= link_to 'Borrar', weight, method: :delete, data: { confirm: 'Estás seguro?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to 'Nuevo Peso', new_weight_path, :class => "btn btn-primary btn-lg" %>
</div>
